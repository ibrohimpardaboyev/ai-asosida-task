import requests
import json

def send_message(message_text):
    pass
    # with open("config.json", "r") as file:
    #     data=json.load(file)
    # bot_token = data['telegram_bot']
    # chat_id = "1505438548"

    # url = f"https://api.telegram.org/bot{bot_token}/sendMessage"

    # payload = {
    #     "chat_id": chat_id,
    #     "text": message_text
    # }

    # response = requests.post(url, data=payload)
# send_message("Hello From Anonymous Bot")